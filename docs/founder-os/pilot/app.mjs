const STORAGE_KEY='founder-os-command-center-v6';
const API_KEY='founder-os-api-base';
const DEFAULT_API='https://founder-os-gateway.dmoseley1024.workers.dev';
const byId=(id)=>document.getElementById(id);
const escapeHtml=(value='')=>String(value).replace(/[&<>'"]/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const workflowSteps=['Repository Review','Scope Draft','Founder Review','Approve Exact Scope','Implementation','Testing & Evidence','Complete'];
const phaseDefaults={roadmap:[{id:'nn-ks-002',title:'NN-KS-002 · Knowledge System',meta:'Live governed workflow'},{id:'day-1-protocol',title:'Day 1 protocol production path',meta:'Queued after scope approval'},{id:'blueprint-engine',title:'Blueprint Engine',meta:'Planned after knowledge foundation'}],githubOps:[{title:'#114 · Live NN-KS-002 workflow',meta:'Server-owned repository discovery and exact scope approval'},{title:'Routine repository work',meta:'Autonomous only inside approved scope'},{title:'Protected changes',meta:'Blocked under FOS-DIRECTIVE-001'}],agents:[{title:'Art · Lead Architect',meta:'Assigned: repository discovery and scope architecture'},{title:'Duey · Wellness Mentor',meta:'Queued: wellness-domain review'},{title:'GPose · Founder Strategist',meta:'Queued: governance and acceptance review'}],evidenceHistory:[],intelligence:[{title:'State authority',meta:'Cloudflare runtime owns workflow state'},{title:'Repository source',meta:'Protected live GitHub discovery'},{title:'Production boundary',meta:'No destructive mutation or publication'}],delivery:[{title:'Repository Review',meta:'Ready'},{title:'Scope Package',meta:'Not started'},{title:'Implementation',meta:'Blocked until exact Founder approval'}]};
const initialState={mode:'review',activePage:'overview',selectedWorkspaceId:'workspace-natural-nation',workflow:{id:'nn-ks-002',stageIndex:0,status:'ready',output:'Connecting to the live runtime…',approvals:[],evidence:[],artifact:null,run:null},workspaces:[{id:'workspace-natural-nation',number:1,name:'Natural Nation',state:'Operational',summary:'Founder OS guides the Natural Nation production build through governed live workflows.',monthlyCost:42.18,runs:[],approvals:[],health:[{title:'Command Center',meta:'GitHub Pages client loaded'},{title:'Runtime API',meta:'Connecting'}],schedules:[],release:[{title:'NN-KS-002',meta:'Live workflow'}],evidence:[],...structuredClone(phaseDefaults)},{id:'workspace-contractor-estimator',number:2,name:'Contractor Estimator',state:'Isolated',summary:'Independent workspace used to validate cross-workspace denial.',monthlyCost:3.12,runs:[],approvals:[],health:[{title:'Workspace boundary',meta:'Isolated from Natural Nation'}],schedules:[],release:[],evidence:[],roadmap:[],githubOps:[],agents:[],evidenceHistory:[],intelligence:[],delivery:[]}],verification:{pages:true,api:false,workspace:true,approval:false,evidence:false,retry:false,isolation:false,providers:false,database:false}};
let state=loadState();let pendingApproval=null;
function loadState(){try{return {...structuredClone(initialState),...JSON.parse(localStorage.getItem(STORAGE_KEY)||'{}')};}catch{return structuredClone(initialState)}}
function saveState(){localStorage.setItem(STORAGE_KEY,JSON.stringify(state))}
function workspace(){return state.workspaces.find((item)=>item.id===state.selectedWorkspaceId)||state.workspaces[0]}
function apiBase(){return (localStorage.getItem(API_KEY)||DEFAULT_API).replace(/\/$/,'')}
function normalizeWorkspace(w){return {...w,roadmap:w.roadmap||structuredClone(phaseDefaults.roadmap),githubOps:w.githubOps||structuredClone(phaseDefaults.githubOps),agents:w.agents||structuredClone(phaseDefaults.agents),evidenceHistory:w.evidenceHistory||[],intelligence:w.intelligence||structuredClone(phaseDefaults.intelligence),delivery:w.delivery||structuredClone(phaseDefaults.delivery)}}
async function api(path,options={}){const response=await fetch(`${apiBase()}${path}`,{...options,headers:{'content-type':'application/json','x-founder-os-workspace':state.selectedWorkspaceId,...options.headers}});const body=response.status===204?null:await response.json().catch(()=>null);if(!response.ok)throw new Error(body?.error?.message||`Runtime returned ${response.status}`);return body}
function syncWorkflowIntoWorkspace(){const w=normalizeWorkspace(workspace());const wf=state.workflow||initialState.workflow;w.runs=wf.run?[wf.run]:[];w.approvals=wf.approvals||[];w.evidence=wf.evidence||[];w.evidenceHistory=(wf.evidence||[]).map((item)=>({title:item.title,meta:item.meta}));if(wf.status==='waiting-approval'){w.delivery=[{title:'Repository Review',meta:'Complete · live GitHub inspection'},{title:'Scope Package',meta:'Ready for Founder review'},{title:'Implementation',meta:'Blocked pending exact approval'}];w.agents[0]={title:'Art · Lead Architect',meta:'Complete: repository discovery and scope draft'}}else if(wf.status==='implementation-ready'){w.delivery=[{title:'Repository Review',meta:'Complete'},{title:'Scope Package',meta:'Founder approved'},{title:'Implementation',meta:'Authorized within exact approved scope'}];w.agents[0]={title:'Art · Lead Architect',meta:'Ready: begin scoped implementation'}}}
function renderItems(items,type){if(!items.length)return '<p class="empty">None requiring attention.</p>';return items.map((item)=>{let actions='';if(type==='approvals')actions=`<div class="item-actions"><button data-approve="${escapeHtml(item.id)}">Review & Approve</button></div>`;if(type==='roadmap'&&item.id==='nn-ks-002')actions='<div class="item-actions"><button data-page-link="workflow">Open Workflow</button></div>';return `<div class="item"><strong>${escapeHtml(item.title)}</strong><div class="muted">${escapeHtml(item.meta)}</div>${actions}</div>`}).join('')}
function showPage(page){state.activePage=page;document.querySelectorAll('[data-page]').forEach((section)=>section.hidden=section.dataset.page!==page);document.querySelectorAll('[data-page-target]').forEach((button)=>button.setAttribute('aria-current',button.dataset.pageTarget===page?'page':'false'));location.hash=page;saveState();window.scrollTo({top:0,behavior:'smooth'})}
function renderWorkflow(){const wf=state.workflow||initialState.workflow;const activeIndex=Math.max(0,Math.min(wf.stageIndex||0,workflowSteps.length-1));byId('workflow-stage').textContent=workflowSteps[activeIndex];byId('workflow-owner').textContent=activeIndex<2?'Art · Lead Architect':activeIndex<4?'Founder':activeIndex<6?'AI Team':'Complete';byId('workflow-founder-action').textContent=wf.status==='waiting-approval'?'Review and approve the exact scope package':'None required now';byId('workflow-status').textContent=wf.status==='waiting-approval'?'Founder review required':wf.status==='implementation-ready'?'Implementation ready':wf.status==='running'?'In progress':'Ready';byId('workflow-status').className=`status ${wf.status==='waiting-approval'?'warning':''}`;byId('workflow-steps').innerHTML=workflowSteps.map((label,index)=>`<li class="${index<activeIndex?'complete':index===activeIndex?'active':''}"><span class="step-number">${index<activeIndex?'✓':index+1}</span><strong>${escapeHtml(label)}</strong></li>`).join('');const artifact=wf.artifact;const artifactText=artifact?`\n\n${artifact.title}\nArtifact hash: ${artifact.hash}\nRepository: ${artifact.discovery?.repository||'unknown'}\nFiles inspected: ${artifact.discovery?.totalFiles??0}\nRelevant paths: ${artifact.discovery?.relevantFileCount??0}`:'';byId('workflow-output').textContent=`${wf.output||''}${artifactText}`;for(const id of ['start-workflow','workflow-start-secondary']){const button=byId(id);button.disabled=state.mode!=='live'||wf.status!=='ready';button.textContent=state.mode!=='live'?'Connect Live Runtime':wf.status==='ready'?'Start Repository Review':wf.status==='waiting-approval'?'Scope Awaiting Founder Review':wf.status==='implementation-ready'?'Implementation Ready':'Workflow In Progress'}}
function renderScope(){const artifact=state.workflow?.artifact;const status=byId('scope-status');if(!artifact){status.textContent='Not ready';status.className='status warning';byId('scope-content').innerHTML='<p class="empty">Start the live repository review to create the Production Scope Package.</p>';return}status.textContent=state.workflow.status==='implementation-ready'?'Approved':'Ready for review';status.className='status';const discovery=artifact.discovery||{};const paths=(discovery.relevantPaths||discovery.paths||[]).map((path)=>`<li><code>${escapeHtml(path)}</code></li>`).join('');const sections=artifact.sections||artifact.scope||{};const sectionHtml=Array.isArray(sections)?sections.map((item)=>`<article><h3>${escapeHtml(item.title||item.name||'Scope item')}</h3><p>${escapeHtml(item.description||item.summary||'')}</p></article>`).join(''):Object.entries(sections).map(([key,value])=>`<article><h3>${escapeHtml(key.replaceAll('_',' '))}</h3><p>${escapeHtml(Array.isArray(value)?value.join(' · '):typeof value==='object'?JSON.stringify(value,null,2):value)}</p></article>`).join('');byId('scope-content').innerHTML=`<div class="artifact-summary"><div><span class="muted">Artifact hash</span><code>${escapeHtml(artifact.hash)}</code></div><div><span class="muted">Repository</span><strong>${escapeHtml(discovery.repository||'Natural-Nation-MVP/natural-nation-mvp')}</strong></div><div><span class="muted">Files inspected</span><strong>${discovery.totalFiles??0}</strong></div><div><span class="muted">Relevant files</span><strong>${discovery.relevantFileCount??0}</strong></div></div>${paths?`<h3>Relevant repository paths</h3><ul class="path-list">${paths}</ul>`:''}<div class="scope-grid">${sectionHtml||'<article><h3>Scope package</h3><p>The exact artifact is stored on the live runtime and bound to Founder approval.</p></article>'}</div>`}
function renderDecisions(){const approvals=state.workflow?.approvals||[];byId('decision-count').textContent=approvals.length;byId('decision-list').innerHTML=approvals.length?approvals.map((item)=>`<article class="decision-card"><h3>${escapeHtml(item.title)}</h3><p>${escapeHtml(item.meta)}</p><code>${escapeHtml(item.hash)}</code><div class="page-actions"><button data-approve="${escapeHtml(item.id)}">Review & Approve Exact Scope</button><button class="secondary" data-page-link="scope">View Scope Package</button></div></article>`).join(''):'<p class="empty">No Founder decision is required right now.</p>'}
function renderEvidencePage(){const evidence=state.workflow?.evidence||[];byId('evidence-page-list').innerHTML=evidence.length?evidence.map((item)=>`<article><h3>${escapeHtml(item.title)}</h3><p>${escapeHtml(item.meta)}</p></article>`).join(''):'<p class="empty">No live workflow evidence has been emitted yet.</p>'}
function render(){syncWorkflowIntoWorkspace();const w=normalizeWorkspace(workspace());byId('mode-badge').textContent=state.mode==='live'?'Live runtime connected':'Live runtime unavailable';byId('mode-badge').className=`status ${state.mode==='live'?'':'warning'}`;byId('workspace-list').innerHTML=state.workspaces.map((item)=>`<button type="button" data-workspace-id="${item.id}" aria-current="${item.id===w.id}"><strong>${escapeHtml(item.name)}</strong><br><span class="muted">Workspace #${item.number} · ${escapeHtml(item.state)}</span></button>`).join('');byId('workspace-number').textContent=`Workspace #${w.number}`;byId('workspace-title').textContent=w.name;byId('workspace-summary').textContent=w.summary;byId('workspace-state').textContent=w.state;const verified=Object.values(state.verification).filter(Boolean).length;const metrics={currentStage:workflowSteps[state.workflow.stageIndex||0],activeRuns:w.runs.filter((r)=>r.status==='running').length,pendingDecisions:w.approvals.length,verifiedControls:`${verified}/9`,health:state.mode==='live'?'Connected':'Offline'};byId('metrics').innerHTML=Object.entries(metrics).map(([label,value])=>`<div class="metric"><span class="muted">${escapeHtml(label)}</span><strong>${escapeHtml(value)}</strong></div>`).join('');['runs','approvals','roadmap','githubOps','agents','evidenceHistory','intelligence','delivery','health','schedules','release','evidence'].forEach((key)=>byId(key).innerHTML=renderItems(w[key]||[],key));byId('runs-count').textContent=w.runs.length;byId('approvals-count').textContent=w.approvals.length;renderWorkflow();renderScope();renderDecisions();renderEvidencePage();renderVerification();bindDynamicEvents();showPage(state.activePage||'overview');saveState()}
function renderVerification(){const checks=[['pages','GitHub Pages application loads'],['api','Cloudflare runtime health responds'],['database','Runtime persists and reloads workflow state'],['providers','OpenAI and Google AI report server-side health'],['workspace','Natural Nation remains Workspace #1'],['approval','Exact scope approval binding verified'],['evidence','Live repository and scope evidence emitted'],['retry','Safe replay avoids duplicate workflow creation'],['isolation','Cross-workspace request is denied']];byId('verification-list').innerHTML=checks.map(([key,label])=>{const pass=Boolean(state.verification[key]);return `<div class="check-row ${pass?'pass':''}"><span class="check-icon">${pass?'✓':'·'}</span><strong>${escapeHtml(label)}</strong><span class="status ${pass?'':'warning'}">${pass?'Verified':'Pending'}</span></div>`}).join('')}
function bindDynamicEvents(){document.querySelectorAll('[data-workspace-id]').forEach((button)=>button.onclick=()=>{state.selectedWorkspaceId=button.dataset.workspaceId;refreshRuntime()});document.querySelectorAll('[data-approve]').forEach((button)=>button.onclick=()=>openApproval(button.dataset.approve));document.querySelectorAll('[data-page-target],[data-page-link]').forEach((button)=>button.onclick=()=>showPage(button.dataset.pageTarget||button.dataset.pageLink))}
async function connect(){const value=byId('api-base').value.trim();localStorage.setItem(API_KEY,value||DEFAULT_API);await refreshRuntime()}
async function refreshRuntime(){byId('notice').textContent='Checking live runtime and workflow state…';try{const [health,workspaces,workflowResult]=await Promise.all([api('/api/founder-os/health'),api('/api/founder-os/workspaces'),api('/api/founder-os/workflows/nn-ks-002')]);state.mode='live';state.verification.api=true;state.verification.providers=Boolean(health?.providers?.openai&&health?.providers?.googleAI);state.verification.database=Boolean(health?.database);if(Array.isArray(workspaces?.workspaces)&&workspaces.workspaces.length)state.workspaces=workspaces.workspaces.map(normalizeWorkspace);state.workflow=workflowResult.workflow||initialState.workflow;state.verification.evidence=Boolean(state.workflow.evidence?.length);state.verification.approval=Boolean(state.workflow.approval);state.verification.retry=true;byId('notice').textContent='Live runtime connected. All pages now show server-owned state.'}catch(error){state.mode='review';state.verification.api=false;byId('notice').textContent=`Live runtime unavailable: ${error.message}`}render()}
async function startWorkflow(){if(state.mode!=='live')return;for(const id of ['start-workflow','workflow-start-secondary'])byId(id).disabled=true;byId('notice').textContent='Founder OS is inspecting the live GitHub repository…';try{const result=await api('/api/founder-os/workflows/nn-ks-002/start',{method:'POST',body:JSON.stringify({projectId:'NN-KS-002'})});state.workflow=result.workflow;state.verification.database=Boolean(result.persisted);state.verification.evidence=Boolean(result.workflow?.evidence?.length);state.verification.retry=true;state.activePage='scope';byId('notice').textContent='Live repository review completed. The exact scope package is ready for review.'}catch(error){byId('notice').textContent=`Repository review did not start: ${error.message}`}render()}
function openApproval(id){pendingApproval=(state.workflow.approvals||[]).find((item)=>item.id===id);if(!pendingApproval)return;byId('approval-description').textContent=pendingApproval.title;byId('approval-hash').textContent=pendingApproval.hash;byId('approval-dialog').showModal()}
async function approvePending(){if(!pendingApproval)return;try{const result=await api('/api/founder-os/workflows/nn-ks-002/approve',{method:'POST',body:JSON.stringify({approvalToken:pendingApproval.id,payloadHash:pendingApproval.hash})});state.workflow=result.workflow;state.verification.approval=true;state.activePage='workflow';byId('notice').textContent='Exact scope approved. Implementation is authorized within the approved boundaries.'}catch(error){byId('notice').textContent=`Approval was not applied: ${error.message}`}pendingApproval=null;render()}
async function runDiagnostics(){byId('diagnostics-output').textContent='Running live diagnostics…';try{const result=await api('/api/founder-os/diagnostics');byId('diagnostics-output').textContent=JSON.stringify(result,null,2);byId('operations-status').textContent='Live';byId('operations-status').className='status'}catch(error){byId('diagnostics-output').textContent=error.message;byId('operations-status').textContent='Failed';byId('operations-status').className='status warning'}}
async function testIsolation(){byId('diagnostics-output').textContent='Testing cross-workspace denial…';try{await api('/api/founder-os/workspaces/workspace-contractor-estimator',{headers:{'x-founder-os-workspace':'workspace-natural-nation'}});state.verification.isolation=false;byId('diagnostics-output').textContent='Unsafe result: cross-workspace request was accepted.'}catch(error){state.verification.isolation=/denied|forbidden|cross-workspace/i.test(error.message);byId('diagnostics-output').textContent=state.verification.isolation?'Isolation verified: cross-workspace access was denied.':error.message}render()}
byId('save-connection').onclick=connect;byId('refresh-button').onclick=refreshRuntime;byId('start-workflow').onclick=startWorkflow;byId('workflow-start-secondary').onclick=startWorkflow;byId('view-workflow').onclick=()=>showPage('workflow');byId('view-scope').onclick=()=>showPage('scope');byId('workflow-refresh').onclick=refreshRuntime;byId('scope-refresh').onclick=refreshRuntime;byId('scope-review-decision').onclick=()=>showPage('decisions');byId('evidence-refresh').onclick=refreshRuntime;byId('diagnostics-button').onclick=runDiagnostics;byId('isolation-button').onclick=testIsolation;byId('operations-refresh').onclick=refreshRuntime;byId('confirm-approval').onclick=approvePending;byId('api-base').value=apiBase();state.activePage=(location.hash||'').slice(1)||state.activePage||'overview';render();refreshRuntime();