{
  "name": "founder-os-gateway",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "test": "find test -name '*.test.mjs' ! -name 'nn-ks-002.test.mjs' -print0 | sort -z | xargs -0 node --test --test-concurrency=1 && node --test test/nn-ks-002.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
