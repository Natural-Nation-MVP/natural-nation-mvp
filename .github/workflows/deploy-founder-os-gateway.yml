name: Deploy Founder OS Gateway

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "services/founder-os-gateway/**"
      - ".github/workflows/deploy-founder-os-gateway.yml"

permissions:
  contents: read

concurrency:
  group: founder-os-gateway-production
  cancel-in-progress: false

jobs:
  validate-and-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/founder-os-gateway

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Run Gateway tests
        run: npm test

      - name: Validate Worker bundle
        run: npm run check

      - name: Verify Cloudflare deployment credentials
        run: |
          test -n "$CLOUDFLARE_API_TOKEN" || { echo "Missing CLOUDFLARE_API_TOKEN repository secret."; exit 1; }
          test -n "$CLOUDFLARE_ACCOUNT_ID" || { echo "Missing CLOUDFLARE_ACCOUNT_ID repository secret."; exit 1; }
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Deploy Worker
        run: npm run deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Verify deployed Worker version
        run: |
          for attempt in 1 2 3 4 5; do
            RESPONSE="$(curl --fail --silent --show-error https://founder-os-gateway.dmoseley1024.workers.dev/version || true)"
            if echo "$RESPONSE" | grep -q '"version":"0.7.0"'; then
              echo "Cloudflare Worker v0.7.0 verified."
              exit 0
            fi
            echo "Worker version not ready yet; retrying in 5 seconds."
            sleep 5
          done
          echo "Cloudflare Worker did not report v0.7.0 after deployment."
          exit 1

      - name: Verify Founder OS health endpoint
        run: |
          RESPONSE="$(curl --fail --silent --show-error https://founder-os-gateway.dmoseley1024.workers.dev/api/founder-os/health)"
          echo "$RESPONSE" | grep -q '"status":"healthy"'
          echo "$RESPONSE" | grep -q '"version":"0.7.0"'
          echo "Founder OS live health endpoint verified."

      - name: Verify NN-KS-002 workflow endpoint
        run: |
          RESPONSE="$(curl --fail --silent --show-error -H 'x-founder-os-workspace: workspace-natural-nation' https://founder-os-gateway.dmoseley1024.workers.dev/api/founder-os/workflows/nn-ks-002)"
          echo "$RESPONSE" | grep -q '"id":"nn-ks-002"'
          echo "$RESPONSE" | grep -q '"workspaceId":"workspace-natural-nation"'
          echo "NN-KS-002 live workflow endpoint verified."
